Mobile body-scan layout stabilization: added mobile-only fixed-height title container for body-scan text steps (from feet prompt through final completion prompt) so header block stays constant and body outline spacing remains stable across one-line/two-line text
