Fix mobile fire sequence positioning: keep mobile PNG-sequence renderer inside the original burn layer/container (no body reparent or mobile global overlay class) so it stays aligned with desktop center and no off-screen scaling drift
